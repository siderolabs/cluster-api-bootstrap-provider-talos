apiVersion: apps/v1
kind: Deployment
metadata:
  name: controller-manager
  namespace: system
spec:
  template:
    spec:
      containers:
      - image: autonomy/cluster-api-talos-controller:latest
        name: manager
        imagePullPolicy: Always
